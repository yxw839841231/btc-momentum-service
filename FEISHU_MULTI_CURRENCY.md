# 飞书多币种合并消息功能

## 功能说明

飞书现在支持将所有币种的分析信息合并为一条富文本消息推送，解决了之前只推送 BTC 的问题。

## 效果对比

### 之前 ❌

- 每个币种发送一条独立消息
- 触发飞书 Webhook 频率限制
- 只有第一条消息（BTC）成功，后续消息失败

### 现在 ✅

- 所有币种合并为一条消息
- 避免频率限制
- 一次发送所有币种信息
- 更美观的展示方式

## 消息样式

### 1. 富文本卡片（推荐，默认）

**特点**：
- 支持多种元素类型（文本、分隔线、按钮）
- 每个币种独立区块
- 清晰的视觉分隔

**格式**：
```
┌────────────────────────────────────┐
│ 📊 多币种分析 (3 个)                │
├────────────────────────────────────┤
│                                    │
│ 📊 多币种动能分析报告                │
│ 更新时间: 2026-01-27 21:00:00      │
│                                    │
│ ────────────────────────────────── │
│                                    │
│ 📈 ₿ BTC                           │
│ 当前价格: $98,456.78               │
│ 24h 涨跌: +1234.56 (+1.27%)        │
│                                    │
│ ────────────────────────────────── │
│                                    │
│ 📈 ◆ SUI                           │
│ 当前价格: $1.50                    │
│ 24h 涨跌: +0.02 (+1.35%)           │
│                                    │
│ ────────────────────────────────── │
│                                    │
│ 📉 ◎ SOL                           │
│ 当前价格: $150.25                  │
│ 24h 涨跌: -3.50 (-2.28%)           │
│                                    │
│ ────────────────────────────────── │
│                                    │
│ [📈 查看所有报告] 按钮              │
│                                    │
└────────────────────────────────────┘
```

### 2. 紧凑表格（可选）

**特点**：
- 表格形式展示
- 适合大量币种
- 更紧凑的布局

**格式**：
```
📊 多币种动能分析

更新时间: 2026-01-27 21:00:00

| 币种 | 价格 | 24h 涨跌 | 涨跌幅 |
|------|------|---------|--------|
| 📈 ₿ BTC | $98,456.78 | +1234.56 | +1.27% |
| 📈 ◆ SUI | $1.50 | +0.02 | +1.35% |
| 📉 ◎ SOL | $150.25 | -3.50 | -2.28% |

[📈 查看完整报告]
```

## 使用方法

### 默认配置（推荐）

系统默认使用富文本卡片格式，无需额外配置：

```json
{
  "analysis": {
    "currencies": ["BTC", "SUI", "SOL"]
  },
  "feishu": {
    "enabled": true,
    "webhook_url": "${FEISHU_WEBHOOK_URL}"
  }
}
```

### 切换到紧凑表格格式

如果币种数量较多（>5个），可以切换到紧凑表格格式。

编辑 `scripts/run_analysis_multi.py` 第 236 行：

```python
send_result = feishu_bot.send_multi_currency_report(
    all_results=all_results,
    index_url=github_pages_url + "/",
    style="compact"  # 改为 "compact"
)
```

## 测试

### 1. 格式预览测试（不需要发送）

```bash
python3 scripts/test_multi_feishu.py
```

### 2. 实际发送测试（需要设置 Webhook）

```bash
export FEISHU_WEBHOOK_URL='your_webhook_url'
python3 scripts/test_multi_feishu.py
```

脚本会显示：
- 两种格式的预览
- 实际发送到飞书（如果设置了环境变量）

## 优势

### 1. 避免限流

**之前**：发送 3 条消息 → 触发限流 → 只有 1 条成功

**现在**：发送 1 条消息 → 不触发限流 → 100% 成功

### 2. 更好的用户体验

- 一条消息包含所有信息
- 一目了然看到所有币种
- 不用翻看多条消息

### 3. 减少打扰

**之前**：
```
[消息1] BTC 分析...
[消息2] SUI 分析...
[消息3] SOL 分析...
```

**现在**：
```
[消息1] 多币种分析 (BTC, SUI, SOL)
```

### 4. 更美观

- 使用富文本卡片
- 支持图标、颜色、分隔线
- 专业的视觉效果

## Telegram vs 飞书

| 特性 | Telegram | 飞书 |
|------|----------|------|
| 推送方式 | 每币种一条独立消息 | 所有币种合并一条消息 |
| 消息数量 | 3 条 | 1 条 |
| 格式 | 纯文本 + Markdown | 富文本卡片 |
| 限流问题 | 无 | 有（已解决） |
| 视觉效果 | 简洁 | 美观 |

## 币种图标

| 币种 | 图标 | 含义 |
|------|------|------|
| BTC | ₿ | 比特币符号 |
| ETH | Ξ | 以太坊符号 |
| SOL | ◎ | Solana 符号 |
| SUI | ◆ | 菱形（新兴币种） |
| DOGE | Ð | Dogecoin 符号 |
| 其他 | ● | 默认圆点 |

## 价格指示器

| 指示器 | 含义 |
|--------|------|
| 📈 | 上涨（24h 涨跌 > 0） |
| 📉 | 下跌（24h 涨跌 < 0） |
| ➡️ | 平盘（24h 涨跌 = 0） |

## 技术实现

### 新增文件

- `src/feishu_multi_formatter.py` - 多币种消息格式化器
- `scripts/test_multi_feishu.py` - 测试脚本

### 修改文件

- `src/feishu_bot.py` - 添加 `send_multi_currency_report()` 和 `send_rich_card()` 方法
- `scripts/run_analysis_multi.py` - 使用合并消息推送

### API 说明

**飞书 Bot 新方法**：

```python
# 发送多币种合并报告
bot.send_multi_currency_report(
    all_results: List[dict],  # 所有币种的分析结果
    index_url: str,           # 索引页面链接
    style: str = "rich"       # 样式: "rich" 或 "compact"
)

# 发送富文本卡片
bot.send_rich_card(
    title: str,              # 卡片标题
    elements: List[dict]     # 卡片元素列表
)
```

## 故障排查

### 问题：飞书仍然只显示 BTC

**检查**：
1. 确认代码已更新到最新版本（commit `00fbbaf` 或更新）
2. 查看 GitHub Actions 日志，确认使用的是合并消息
3. 检查飞书群是否收到消息

### 问题：消息格式显示异常

**解决**：
1. 飞书客户端版本可能过旧，更新到最新版本
2. 尝试使用 `compact` 样式
3. 查看错误日志

### 问题：按钮无法点击

**检查**：
1. 确认 `index_url` 配置正确
2. 检查 URL 是否可以访问
3. 飞书客户端可能限制某些链接

## 对比示例

### 之前（逐条推送）

```
飞书群消息：

[消息1 - 21:00:01]
📊 BTC 动能分析报告
💰 ₿ BTC 当前价格
$98,456.78
...

[消息2 - 21:00:03] （可能被限流拒绝）
📊 SUI 动能分析报告
...

[消息3 - 21:00:05] （可能被限流拒绝）
📊 SOL 动能分析报告
...
```

### 现在（合并推送）

```
飞书群消息：

[消息1 - 21:00:01]
┌──────────────────────────┐
│ 📊 多币种分析 (3 个)      │
├──────────────────────────┤
│ 📈 ₿ BTC                 │
│ $98,456.78 (+1.27%)      │
│ ──────────────────────── │
│ 📈 ◆ SUI                 │
│ $1.50 (+1.35%)           │
│ ──────────────────────── │
│ 📉 ◎ SOL                 │
│ $150.25 (-2.28%)         │
│ ──────────────────────── │
│ [📈 查看所有报告]        │
└──────────────────────────┘
```

## 总结

✅ **已解决**：飞书只推送 BTC 的问题

✅ **新功能**：多币种合并消息，一条消息显示所有币种

✅ **优化**：避免限流，提升用户体验

✅ **美化**：富文本卡片，更专业的展示

下次 GitHub Actions 运行时，飞书将收到包含所有币种的单条消息！🎉
