# 新功能使用指南

## 功能概述

本次更新添加了两个重要功能：

1. ✅ **CoinGecko API 集成** - 获取 SUI 等小币种实时价格
2. ✅ **价格预警系统** - 自动监控价格变化并发送告警

---

## 功能 1: CoinGecko API 集成

### 问题描述

**之前**：
- OKX/Binance 不支持 SUI 交易对
- SUI 使用模拟价格（$1.50 ± 2%）
- 无法获取实时价格

**现在**：
- 集成 CoinGecko API
- 支持 100+ 币种的实时价格
- 四级容错：OKX → Binance → CoinGecko → 模拟数据

### 数据获取优先级

```
1️⃣ OKX API（实时）
   ↓ 失败
2️⃣ Binance API（实时）
   ↓ 失败
3️⃣ CoinGecko API（实时）✨ 新增
   ↓ 失败
4️⃣ 模拟数据（离线）
```

### 支持的币种

CoinGecko 支持以下币种（实时价格）：

| 币种 | CoinGecko ID | 状态 |
|------|--------------|------|
| BTC | bitcoin | ✅ |
| ETH | ethereum | ✅ |
| SOL | solana | ✅ |
| **SUI** | **sui** | **✅ 新增** |
| BNB | binancecoin | ✅ |
| XRP | ripple | ✅ |
| ADA | cardano | ✅ |
| DOGE | dogecoin | ✅ |
| DOT | polkadot | ✅ |
| MATIC | matic-network | ✅ |
| AVAX | avalanche-2 | ✅ |

### 使用方法

无需配置，自动启用！

**价格数据日志**：

**之前**：
```
⚠️  SUI 使用模拟价格: $1.50 [Simulated]
```

**现在**：
```
✅ SUI 价格: $1.4856 (+1.35%) [CoinGecko]
```

### API 限制

- **免费额度**: 10-30 calls/minute
- **无需 API Key**: 免费版足够使用
- **数据来源**: 聚合多个交易所

---

## 功能 2: 价格预警系统

### 功能特性

自动监控价格变化，在满足条件时发送告警：

#### 1. 大幅波动告警 ⚡

监控 24 小时涨跌幅，超过阈值时告警。

**默认阈值**: ±5%

**示例**：
```
🟠 **BTC** 24h暴涨 6.50%
🟠 **SUI** 24h暴跌 7.20%
```

#### 2. 价格目标告警 🎯

突破或跌破关键价位时告警。

**默认配置**:
- BTC 上限: $100,000
- BTC 下限: $90,000

**示例**：
```
🟡 **BTC** 突破价格目标 $100,000
🟡 **BTC** 跌破价格目标 $90,000
```

### 告警严重程度

| 级别 | 图标 | 条件 |
|------|------|------|
| 🔴 紧急 | CRITICAL | 24h 涨跌 ≥ 10% |
| 🟠 高 | HIGH | 24h 涨跌 ≥ 5% |
| 🟡 中 | MEDIUM | 价格目标触发 |
| 🔵 低 | LOW | 其他告警 |

### 告警冷却机制

**目的**: 避免重复告警，防止消息轰炸

**冷却时间**: 60 分钟（默认）

**示例**：
```
09:00 - BTC 大幅波动告警（+6.5%）
09:05 - 再次检查 → 在冷却期，不发送告警 ✅
10:00 - 冷却期结束，可以再次告警
```

### 推送方式

#### Telegram 推送

**币种分析推送**：
```
📊 BTC 动能分析
💰 ₿ BTC 价格: 📈 $98,456.78
24h: +123.45 (+1.27%)
...
```

**告警汇总推送**（独立消息）：
```
🚨 价格预警汇总

• 🟠 **BTC** 24h暴涨 6.50%
• 🟡 **SUI** 跌破价格目标 $1.45

时间: 2026-01-27 21:00:00
```

#### 飞书推送

告警会自动添加到多币种合并消息中。

### 配置价格目标

#### 方法 1: 修改代码（临时）

编辑 `src/alert_manager.py`:

```python
self.config = {
    "large_move_threshold": 5.0,     # 大幅波动阈值
    "price_targets": {
        "BTC": {"high": 100000, "low": 90000},
        "ETH": {"high": 4000, "low": 3000},
        "SUI": {"high": 2.0, "low": 1.3}
    }
}
```

#### 方法 2: 运行时设置（推荐）

创建配置脚本：

```python
# scripts/set_price_targets.py
from src.alert_manager import AlertManager

manager = AlertManager()
manager.set_price_target("BTC", high=100000, low=90000)
manager.set_price_target("ETH", high=4000, low=3000)
manager.set_price_target("SUI", high=2.0, low=1.3)
```

### 测试

运行测试脚本验证功能：

```bash
python3 scripts/test_new_features.py
```

**测试内容**：
1. ✅ CoinGecko API 价格获取
2. ✅ 大幅波动告警（±5%）
3. ✅ 价格目标告警
4. ✅ 告警冷却机制

### 告警示例

#### 场景 1: 大幅波动

**数据**：
- BTC 24h 涨跌: +6.5%

**告警消息**：
```
🚨 价格预警汇总

• 🟠 **BTC** 24h暴涨 6.50%

时间: 2026-01-27 21:00:00
```

#### 场景 2: 价格目标突破

**数据**：
- BTC 当前价格: $101,500
- 目标上限: $100,000

**告警消息**：
```
🚨 价格预警汇总

• 🟡 **BTC** 突破价格目标 $100,000

时间: 2026-01-27 21:00:00
```

#### 场景 3: 无告警（正常波动）

**数据**：
- BTC 24h 涨跌: +2.0%
- 当前价格: $98,000

**结果**：不发送告警消息

### 禁用告警

如果暂时不需要告警功能，可以在 `scripts/run_analysis_multi.py` 中注释掉告警检查代码：

```python
# 检查价格预警
logger.info(f"🚨 检查价格预警...")
# alert_manager = AlertManager()
# all_alerts = []
# ... (告警检查代码)
logger.info(f"✅ 告警功能已禁用")
```

---

## 技术实现

### 文件结构

```
src/
├── multi_currency_fetcher.py  # 添加 CoinGecko API
│   ├── _fetch_from_coingecko()      # 新方法
│   └── _format_price_data_coingecko()  # 新方法
│
├── alert_manager.py           # 价格预警管理器（新文件）
│   ├── AlertManager                  # 主类
│   ├── PriceAlert                   # 告警对象
│   ├── AlertType                    # 告警类型枚举
│   └── AlertSeverity                # 严重程度枚举
│
scripts/
├── run_analysis_multi.py     # 集成告警功能
└── test_new_features.py      # 测试脚本（新文件）
```

### CoinGecko API 调用

```python
# API 端点
url = "https://api.coingecko.com/api/v3/simple/price"

# 请求参数
params = {
    "ids": "sui",              # 币种 ID
    "vs_currencies": "usd",      # 计价货币
    "include_24hr_change": "true"  # 包含24h变化
}

# 响应示例
{
  "sui": {
    "usd": 1.4856,
    "usd_24h_change": 1.35
  }
}
```

### 告警检查流程

```
分析完成
  ↓
获取价格数据
  ↓
检查告警条件
  ├─ 大幅波动? → 触发告警
  ├─ 价格目标? → 触发告警
  └─ 技术指标? → 触发告警
  ↓
检查冷却时间
  ├─ 在冷却期 → 跳过
  └─ 冷却结束 → 发送告警
  ↓
推送到 Telegram/飞书
```

---

## 使用建议

### 1. 价格目标配置

**推荐配置**：

```python
# 主流币种
BTC: high=100000, low=90000   # 关键心理价位
ETH: high=4000, low=3000

# 小币种（波动大）
SUI: high=2.0, low=1.3
SOL: high=200, low=120
```

### 2. 大幅波动阈值

**建议**：
- 保守: ±3%（减少告警频率）
- 平衡: ±5%（默认）
- 激进: ±8%（捕捉更多机会）

### 3. 告警冷却时间

**建议**：
- 短期: 30 分钟（及时但可能重复）
- 默认: 60 分钟（平衡）
- 长期: 120 分钟（减少打扰）

---

## 常见问题

### Q1: CoinGecko API 会超限吗？

**A**: 免费版限制 10-30 calls/minute，我们的使用频率很低（每小时 3-4 次），完全够用。

### Q2: 如何只接收重要告警？

**A**:
1. 提高 `large_move_threshold` 到 ±8%
2. 只配置关键币种的价格目标
3. 增加冷却时间到 120 分钟

### Q3: 告警会影响性能吗？

**A**: 不会。告警检查非常快速（<1秒），几乎无性能影响。

### Q4: 可以通过 Telegram 命令配置价格目标吗？

**A**: 当前版本不支持。需要修改代码或配置文件。未来可以添加这个功能。

### Q5: CoinGecko 价格准确吗？

**A**: CoinGecko 聚合了多个交易所的价格，数据准确可靠。对于 SUI 等小币种，是最佳选择。

---

## 对比总结

### CoinGecko API

| 项目 | 之前 | 现在 |
|------|------|------|
| SUI 价格 | 模拟数据 | ✅ 实时数据 |
| 支持币种 | 11个（OKX/Binance） | ✅ 100+ 个 |
| 数据来源 | 2个交易所 | ✅ 3个交易所 |
| 准确性 | 低 | ✅ 高 |

### 价格预警

| 功能 | 状态 |
|------|------|
| 大幅波动告警 | ✅ 已实现 |
| 价格目标告警 | ✅ 已实现 |
| 告警冷却 | ✅ 已实现 |
| Telegram 推送 | ✅ 已实现 |
| 飞书推送 | ✅ 已实现 |
| 严重程度分级 | ✅ 已实现 |

---

## 后续优化

### 短期（1周内）

- [ ] 添加配置文件支持价格目标
- [ ] 支持更多告警类型（背离、金叉等）
- [ ] 邮件告警推送

### 中期（1月内）

- [ ] Web界面配置告警
- [ ] 告警历史记录和查询
- [ ] 自定义告警规则

### 长期

- [ ] 机器学习预测告警
- [ ] 社交媒体情绪告警
- [ ] 智能告警频率调整

---

## 总结

✅ **CoinGecko API**: 解决 SUI 等小币种价格问题
✅ **价格预警**: 自动监控，及时告警
✅ **易用性**: 无需配置，自动启用
✅ **可控性**: 冷却机制避免打扰

**下次运行时，系统将自动**：
1. 从 CoinGecko 获取 SUI 等小币种实时价格
2. 检查价格预警条件
3. 发送告警汇总到 Telegram/飞书

🎉 享受实时价格和智能预警！
